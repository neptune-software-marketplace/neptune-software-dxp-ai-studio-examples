{
	"id": "963dcd7b-10c6-f011-8194-000d3ab7ae91",
	"createdAt": "2025-11-20T12:57:40.396Z",
	"createdBy": "edip.sopa@neptune-software.com",
	"config": {
		"entityId": "3299afd2-0816-ec11-ae72-0050f255283a",
		"operations": {
			"C": {
				"enabled": true,
				"name": "Create",
				"description": "Insert new records"
			},
			"D": {
				"enabled": true,
				"name": "Delete",
				"description": "Remove records"
			},
			"R": {
				"enabled": true,
				"name": "Read",
				"description": "Query and view records",
				"fields": [
					{
						"fieldName": "id",
						"fieldType": "uuid",
						"isUnique": true,
						"isNullable": false,
						"generationStrategy": "uuid",
						"included": true
					},
					{
						"fieldName": "createdAt",
						"fieldType": "bigint",
						"isNullable": false,
						"included": true
					},
					{
						"fieldName": "createdBy",
						"fieldType": "text",
						"isNullable": false,
						"included": true
					},
					{
						"fieldName": "updatedAt",
						"fieldType": "bigint",
						"isNullable": false,
						"included": true
					},
					{
						"fieldName": "updatedBy",
						"fieldType": "text",
						"isNullable": false,
						"included": true
					},
					{
						"id": "2e4cccad-e719-4a21-85f2-225baaad0790",
						"fieldName": "name",
						"fieldType": "text",
						"isNullable": true,
						"included": true
					},
					{
						"id": "9c8b4e75-83e4-4605-a00a-d060cc186305",
						"fieldName": "part_number",
						"fieldType": "text",
						"isNullable": true,
						"included": true
					},
					{
						"id": "fef182c1-9d31-41d1-a75b-c4c4e06e25f4",
						"fieldName": "latitude",
						"fieldType": "integer",
						"isNullable": true,
						"included": true
					},
					{
						"id": "5e1d6d88-12a6-4c95-816e-cf24bc6a53f8",
						"fieldName": "longitude",
						"fieldType": "integer",
						"isNullable": true,
						"included": true
					},
					{
						"id": "766198a9-345d-4b9c-a4dd-ceb6aa8c87ea",
						"fieldName": "checkup_date",
						"fieldType": "timestamp",
						"isNullable": true,
						"included": true
					},
					{
						"id": "10759ff4-c100-455f-8eff-4a1cf9746018",
						"fieldName": "checkup_interval",
						"fieldType": "integer",
						"isNullable": true,
						"included": true
					},
					{
						"id": "6ae1e4f7-848b-439f-fb83-3a42d3f6be77",
						"fieldName": "days_until_check",
						"fieldType": "integer",
						"description": "Maintained automatically",
						"isNullable": true,
						"included": true
					},
					{
						"id": "5f02b118-1e1a-40f1-a2b4-a0d4b503ed2d",
						"fieldName": "assigned_for_checking",
						"fieldType": "boolean",
						"isNullable": true,
						"included": true
					},
					{
						"id": "a807b742-38a8-43c1-a282-b027bdb965d6",
						"fieldName": "assigned_to",
						"fieldType": "text",
						"isNullable": true,
						"included": true
					},
					{
						"id": "a408e4a3-bec2-4116-ee75-796719864e1a",
						"fieldName": "pdf",
						"fieldType": "text",
						"isNullable": true,
						"included": true
					}
				]
			},
			"U": {
				"enabled": true,
				"name": "Update",
				"description": "Modify existing records",
				"fields": [
					{
						"id": "2e4cccad-e719-4a21-85f2-225baaad0790",
						"fieldName": "name",
						"fieldType": "text",
						"isNullable": true,
						"included": true
					},
					{
						"id": "9c8b4e75-83e4-4605-a00a-d060cc186305",
						"fieldName": "part_number",
						"fieldType": "text",
						"isNullable": true,
						"included": true
					},
					{
						"id": "fef182c1-9d31-41d1-a75b-c4c4e06e25f4",
						"fieldName": "latitude",
						"fieldType": "integer",
						"isNullable": true,
						"included": true
					},
					{
						"id": "5e1d6d88-12a6-4c95-816e-cf24bc6a53f8",
						"fieldName": "longitude",
						"fieldType": "integer",
						"isNullable": true,
						"included": true
					},
					{
						"id": "766198a9-345d-4b9c-a4dd-ceb6aa8c87ea",
						"fieldName": "checkup_date",
						"fieldType": "timestamp",
						"isNullable": true,
						"included": true
					},
					{
						"id": "10759ff4-c100-455f-8eff-4a1cf9746018",
						"fieldName": "checkup_interval",
						"fieldType": "integer",
						"isNullable": true,
						"included": true
					},
					{
						"id": "6ae1e4f7-848b-439f-fb83-3a42d3f6be77",
						"fieldName": "days_until_check",
						"fieldType": "integer",
						"description": "Maintained automatically",
						"isNullable": true,
						"included": true
					},
					{
						"id": "5f02b118-1e1a-40f1-a2b4-a0d4b503ed2d",
						"fieldName": "assigned_for_checking",
						"fieldType": "boolean",
						"isNullable": true,
						"included": true
					},
					{
						"id": "a807b742-38a8-43c1-a282-b027bdb965d6",
						"fieldName": "assigned_to",
						"fieldType": "text",
						"isNullable": true,
						"included": true
					},
					{
						"id": "a408e4a3-bec2-4116-ee75-796719864e1a",
						"fieldName": "pdf",
						"fieldType": "text",
						"isNullable": true,
						"included": true
					}
				]
			}
		}
	},
	"name": "example-equipment-Table-Tool",
	"ver": "25.11.21.1629",
	"version": null,
	"description": [
		"==OBJECT STRING==",
		"# **Tool Instructions: `equipment`**\r",
		"\r",
		"You are the only data-access tool for the `equipment` table.\r",
		"\r",
		"Your responsibilities:\r",
		"1. Read equipment records  \r",
		"2. Create equipment records  \r",
		"3. Update any equipment field  \r",
		"4. Delete equipment records  \r",
		"\r",
		"You return **structured row data only**.  \r",
		"The **agent** is responsible for turning results into natural language.\r",
		"\r",
		"---\r",
		"\r",
		"## 1. Table Schema\r",
		"\r",
		"Use these field names in filters and updates:\r",
		"\r",
		"- `id` – unique identifier (primary key)  \r",
		"- `name` – equipment name  \r",
		"- `part_number` – equipment / part number  \r",
		"- `checkup_date` – last maintenance date  \r",
		"- `checkup_interval` – interval in days  \r",
		"- `days_until_check` – remaining days (may be negative if overdue)  \r",
		"- `assigned_for_checking` – boolean flag  \r",
		"- `assigned_to` – person responsible  \r",
		"- `latitude` – last known latitude (optional)  \r",
		"- `longitude` – last known longitude (optional)  \r",
		"- `pdf` – link to related PDF (optional)  \r",
		"\r",
		"Backend column names may be prefixed (e.g. `equipment_name`), but you must always work with the logical field names above.  \r",
		"Treat `id` as the **unique** field for single-record operations.\r",
		"\r",
		"---\r",
		"\r",
		"## 2. READ Records\r",
		"\r",
		"You may search using any field.\r",
		"\r",
		"**Searchable fields** (typical):  \r",
		"`name`, `part_number`, `assigned_to`, `checkup_date`, `checkup_interval`, `days_until_check`\r",
		"\r",
		"**Rules:**\r",
		"\r",
		"- Always include `id` in `select` when reading records.  \r",
		"- If no record matches → return an empty `data` array.  \r",
		"- If multiple records match → return all (respecting `limit`).  \r",
		"\r",
		"**Distinct / count rules (to avoid errors):**\r",
		"\r",
		"- For normal data reads: set  \r",
		"  `count = false` and `distinct = false`.  \r",
		"- You may use `count = true` (optionally with `distinct = true`) **only when you actually want a count**.  \r",
		"- **Never** call the tool with `count = false` and `distinct = true`. That combination is not supported and will cause an error.\r",
		"\r",
		"---\r",
		"\r",
		"## 3. CREATE Records\r",
		"\r",
		"You may create new equipment entries.\r",
		"\r",
		"**Required fields:**\r",
		"\r",
		"- `name`  \r",
		"- `part_number`  \r",
		"- `checkup_date`  \r",
		"- `checkup_interval`  \r",
		"- `latitude`\r",
		"- `longitude`\r",
		"\r",
		"Other fields are optional and may be included if provided.\r",
		"\r",
		"On create:\r",
		"1. Insert the record with the given values.  \r",
		"2. The system generates a new `id`.  \r",
		"3. Return the created record including `id` and all stored fields.\r",
		"\r",
		"---\r",
		"\r",
		"## 4. UPDATE Records (ALL FIELDS ALLOWED)\r",
		"\r",
		"You may update **any** field in the table.\r",
		"\r",
		"### 4.1 General rule\r",
		"\r",
		"- You may search by any field (e.g. `name`, `part_number`, …).  \r",
		"- But **single-record updates must always use the unique `id` in the final update call.**\r",
		"\r",
		"### 4.2 Required workflow for single updates\r",
		"\r",
		"1. **Read step**  \r",
		"   - Use any filters the agent provides (e.g. `name = \"Flame programmer\"`).  \r",
		"   - Return records including their `id`.\r",
		"\r",
		"2. **Disambiguation**  \r",
		"   - If more than one row matches, return all and let the agent decide which `id` to use.\r",
		"\r",
		"3. **Update step**  \r",
		"   - Call the update with `updateMode: \"single\"`.  \r",
		"   - Use a filter **only on `id`**:  \r",
		"     `filters: [{ \"fieldName\": \"id\", \"value\": \"<ID>\", \"operator\": \"eq\" }]`  \r",
		"   - Put all fields to change into `data`.\r",
		"\r",
		"4. Return the updated row (or an `affected` count, depending on the standard table-tool behavior).\r",
		"\r",
		"### 4.3 Forbidden patterns\r",
		"\r",
		"- Do **not** call `updateMode: \"single\"` filtered by non-unique fields such as `name`, `part_number`, `assigned_to`, etc.  \r",
		"- Do **not** send a second, redundant update for the same change after a successful update by `id`.\r",
		"\r",
		"### 4.4 Bulk updates\r",
		"\r",
		"For intentional bulk changes:\r",
		"\r",
		"1. Read all matching rows first.  \r",
		"2. Collect their `id`s.  \r",
		"3. Use the appropriate bulk-update mode supported by the table tool.  \r",
		"\r",
		"On every CREATE or UPDATE action, the tool must always return a complete, human-readable summary of the equipment record. The summary must include all key fields: name, part_number, latitude, longitude, checkup_date, checkup_interval, assigned_for_checking, and assigned_to.\r",
		"The agent will then use this output to present a final confirmation message to the user.\r",
		"\r",
		"---\r",
		"\r",
		"## 5. DELETE Records\r",
		"\r",
		"For deletes:\r",
		"\r",
		"1. Read using any filter to find the correct record.  \r",
		"2. Extract its `id`.  \r",
		"3. Delete using a filter only on `id`.  \r",
		"4. Return information that a record with that `id` was deleted.\r",
		"\r",
		"---\r",
		"\r",
		"## 6. Output Behavior\r",
		"\r",
		"- Always return structured data following the standard table-tool shape:  \r",
		"  typically `{ message, data, total }` for reads or `{ message, data: { affected } }` for writes.  \r",
		"- Do **not** try to speak to the end user.  \r",
		"- The **agent** will interpret your result and present it in natural language."
	],
	"package": "3d0574f1-5eba-f011-8194-0022489bf196",
	"type": "TABLE",
	"prompt": [
		"==OBJECT STRING==",
		"# **Tool Instructions: `equipment`**\r",
		"\r",
		"You are the only data-access tool for the `equipment` table.\r",
		"\r",
		"Your responsibilities:\r",
		"1. Read equipment records  \r",
		"2. Create equipment records  \r",
		"3. Update any equipment field  \r",
		"4. Delete equipment records  \r",
		"\r",
		"You return **structured row data only**.  \r",
		"The **agent** is responsible for turning results into natural language.\r",
		"\r",
		"---\r",
		"\r",
		"## 1. Table Schema\r",
		"\r",
		"Use these field names in filters and updates:\r",
		"\r",
		"- `id` – unique identifier (primary key)  \r",
		"- `name` – equipment name  \r",
		"- `part_number` – equipment / part number  \r",
		"- `checkup_date` – last maintenance date  \r",
		"- `checkup_interval` – interval in days  \r",
		"- `days_until_check` – remaining days (may be negative if overdue)  \r",
		"- `assigned_for_checking` – boolean flag  \r",
		"- `assigned_to` – person responsible  \r",
		"- `latitude` – last known latitude (optional)  \r",
		"- `longitude` – last known longitude (optional)  \r",
		"- `pdf` – link to related PDF (optional)  \r",
		"\r",
		"Backend column names may be prefixed (e.g. `equipment_name`), but you must always work with the logical field names above.  \r",
		"Treat `id` as the **unique** field for single-record operations.\r",
		"\r",
		"---\r",
		"\r",
		"## 2. READ Records\r",
		"\r",
		"You may search using any field.\r",
		"\r",
		"**Searchable fields** (typical):  \r",
		"`name`, `part_number`, `assigned_to`, `checkup_date`, `checkup_interval`, `days_until_check`\r",
		"\r",
		"**Rules:**\r",
		"\r",
		"- Always include `id` in `select` when reading records.  \r",
		"- If no record matches → return an empty `data` array.  \r",
		"- If multiple records match → return all (respecting `limit`).  \r",
		"\r",
		"**Distinct / count rules (to avoid errors):**\r",
		"\r",
		"- For normal data reads: set  \r",
		"  `count = false` and `distinct = false`.  \r",
		"- You may use `count = true` (optionally with `distinct = true`) **only when you actually want a count**.  \r",
		"- **Never** call the tool with `count = false` and `distinct = true`. That combination is not supported and will cause an error.\r",
		"\r",
		"---\r",
		"\r",
		"## 3. CREATE Records\r",
		"\r",
		"You may create new equipment entries.\r",
		"\r",
		"**Required fields:**\r",
		"\r",
		"- `name`  \r",
		"- `part_number`  \r",
		"- `checkup_date`  \r",
		"- `checkup_interval`  \r",
		"- `latitude`\r",
		"- `longitude`\r",
		"\r",
		"Other fields are optional and may be included if provided.\r",
		"\r",
		"On create:\r",
		"1. Insert the record with the given values.  \r",
		"2. The system generates a new `id`.  \r",
		"3. Return the created record including `id` and all stored fields.\r",
		"\r",
		"---\r",
		"\r",
		"## 4. UPDATE Records (ALL FIELDS ALLOWED)\r",
		"\r",
		"You may update **any** field in the table.\r",
		"\r",
		"### 4.1 General rule\r",
		"\r",
		"- You may search by any field (e.g. `name`, `part_number`, …).  \r",
		"- But **single-record updates must always use the unique `id` in the final update call.**\r",
		"\r",
		"### 4.2 Required workflow for single updates\r",
		"\r",
		"1. **Read step**  \r",
		"   - Use any filters the agent provides (e.g. `name = \"Flame programmer\"`).  \r",
		"   - Return records including their `id`.\r",
		"\r",
		"2. **Disambiguation**  \r",
		"   - If more than one row matches, return all and let the agent decide which `id` to use.\r",
		"\r",
		"3. **Update step**  \r",
		"   - Call the update with `updateMode: \"single\"`.  \r",
		"   - Use a filter **only on `id`**:  \r",
		"     `filters: [{ \"fieldName\": \"id\", \"value\": \"<ID>\", \"operator\": \"eq\" }]`  \r",
		"   - Put all fields to change into `data`.\r",
		"\r",
		"4. Return the updated row (or an `affected` count, depending on the standard table-tool behavior).\r",
		"\r",
		"### 4.3 Forbidden patterns\r",
		"\r",
		"- Do **not** call `updateMode: \"single\"` filtered by non-unique fields such as `name`, `part_number`, `assigned_to`, etc.  \r",
		"- Do **not** send a second, redundant update for the same change after a successful update by `id`.\r",
		"\r",
		"### 4.4 Bulk updates\r",
		"\r",
		"For intentional bulk changes:\r",
		"\r",
		"1. Read all matching rows first.  \r",
		"2. Collect their `id`s.  \r",
		"3. Use the appropriate bulk-update mode supported by the table tool.  \r",
		"\r",
		"On every CREATE or UPDATE action, the tool must always return a complete, human-readable summary of the equipment record. The summary must include all key fields: name, part_number, latitude, longitude, checkup_date, checkup_interval, assigned_for_checking, and assigned_to.\r",
		"The agent will then use this output to present a final confirmation message to the user.\r",
		"\r",
		"---\r",
		"\r",
		"## 5. DELETE Records\r",
		"\r",
		"For deletes:\r",
		"\r",
		"1. Read using any filter to find the correct record.  \r",
		"2. Extract its `id`.  \r",
		"3. Delete using a filter only on `id`.  \r",
		"4. Return information that a record with that `id` was deleted.\r",
		"\r",
		"---\r",
		"\r",
		"## 6. Output Behavior\r",
		"\r",
		"- Always return structured data following the standard table-tool shape:  \r",
		"  typically `{ message, data, total }` for reads or `{ message, data: { affected } }` for writes.  \r",
		"- Do **not** try to speak to the end user.  \r",
		"- The **agent** will interpret your result and present it in natural language."
	]
}